spring:
  kafka:
    admin:
      properties:
        spring:
          json:
            value.default.type: net.zerotodev.api.consumer.domain.User
kafka:
  topic: test
  groupId: testGroupId
  application:
    name: kafka-consumer
server:
  port: 9091
